# Metrics

The scripts used to compute the metrics and plot the figures presented in the paper.

## Instalation

The requirements can be intalled in a Conda environment as follows:

    conda create -n md4m_metrics python==3.10
    conda activate md4m_calibration
    pip install -r requirements.txt