# OpenMVG Calibration + Depth correction

The requirements can be intalled in a Conda environment as follows:

    conda create -n md4m_calibration python==3.11
    conda activate md4m_calibration
    pip install -r requirements.txt

To run the software:

    chmod +x run_calibration.sh
    ./run_calibration.sh

TODO: explain each script and their options/outputs